#include <bits/stdc++.h>
using namespace std;
int main(){
    // UN-ORDERD set every op cost o(1) in avg and o(n) in worst
    // unordered_set<int> st;
    // st.insert(1);
    // st.insert(3);
    // st.insert(2);
    // for(auto it=st.begin();it!=st.end();it++){
    //     cout<<*it<<" ";
    // }
    // if(st.find(3)!=st.end()){
    //     cout<<"\nWe got the number in set!"<<endl;
    // }

    // VECTORS
    // vector<int> vec;
    //    // Initially: capacity = 0, size = 0
    // std::cout << "Initial - Size: " << vec.size() << ", Capacity: " << vec.capacity() << std::endl;
    
    // // Add elements and watch capacity grow
    // for (int i = 1; i <= 10; i++) {
    //     vec.push_back(i * 10);
    //     std::cout << "After adding " << i*10 << " - Size: " << vec.size() 
    //               << ", Capacity: " << vec.capacity() << std::endl;
                
    // }
    // // overrding using insert
    // vec.insert(vec.begin()+vec.size()-1,23);

    //     for(auto it=vec.begin();it!=vec.end();it++){
    //     cout<<*it<<" ";
    // }


//     set<int> s;
//     s.insert(2);
//     s.insert(1);

//   for(auto it=s.begin();it!=s.end();it++){
//         cout<<*it<<" ";
//     }

    //  map<int,string> m;
    //  m.insert({1,"abc"});
    //  m.insert({1,"bc"});
    //  for(auto x :m){
    //     cout<<x.second<<"\n";
    //  }
    //  cout<<m[2]<<endl;
    //  m.erase(2);
}